<%= form_for(@extra) do |f| %>
  <div class="blue-text">
    <% if @extra.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@extra.errors.count, "error") %> prohibited this extra from being saved:</h2>

        <ul>
        <% @extra.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.text_field :kind, :placeholder=>"Kind" %>
    </div>
    <div class="field">
      <%= f.number_field :money, :placeholder=>"Money" %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>
